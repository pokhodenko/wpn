#!/usr/bin/env bash


if [ ! -f .env ]; then
  echo "Error: .env file not found in the project root." >&2
  exit 1
fi

set -a
source .env
set +a

if [ -z "${PUBLISH_TOKEN:-}" ]; then
  echo "Error: PUBLISH_TOKEN is not set in .env." >&2
  exit 1
fi

./gradlew publishPlugin